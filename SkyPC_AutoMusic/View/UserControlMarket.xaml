<UserControl x:Class="SkyPC_AutoMusic.View.UserControlMarket"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SkyPC_AutoMusic.View"
             xmlns:loc="clr-namespace:SkyPC_AutoMusic.Properties"
             xmlns:theme="clr-namespace:SkyPC_AutoMusic.Theme"
             xmlns:viewmodel="clr-namespace:SkyPC_AutoMusic.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:MarketViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600" d:Background="Transparent">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--标题与状态-->
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource PageTitle}" Text="{x:Static loc:Resources.Market_Header}"/>
            <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0" Style="{StaticResource MutedText}" TextTrimming="CharacterEllipsis" Text="{Binding StatusText}"/>
        </Grid>

        <!--工具栏-->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" Width="110" VerticalAlignment="Center" Margin="0,0,6,0" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup, Mode=TwoWay}" materialDesign:HintAssist.Hint="{x:Static loc:Resources.Market_Category}"/>
                <ComboBox Grid.Column="1" Width="110" VerticalAlignment="Center" Margin="0,0,6,0" ItemsSource="{Binding Subs}" SelectedItem="{Binding SelectedSub, Mode=TwoWay}" materialDesign:HintAssist.Hint="{x:Static loc:Resources.Market_SubCategory}"/>
                <TextBox Grid.Column="2" MinWidth="100" VerticalAlignment="Center" Margin="0,0,6,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="{x:Static loc:Resources.Market_SearchHint}" materialDesign:TextFieldAssist.HasClearButton="True" Style="{StaticResource MaterialDesignFilledTextBox}"/>
                <Button Grid.Column="3" Style="{StaticResource GlassIconSurface}" theme:UiMotion.PressScale="True" Margin="4,0" Foreground="{DynamicResource TextPrimaryBrush}" Command="{Binding OpenRepoCommand}" ToolTip="{x:Static loc:Resources.Market_Repo}" Cursor="Hand">
                    <materialDesign:PackIcon Kind="Github"/>
                </Button>
                <Button Grid.Column="4" Style="{StaticResource GlassIconSurface}" theme:UiMotion.PressScale="True" Margin="4,0" Foreground="{DynamicResource AccentBrush}" Command="{Binding RefreshCommand}" ToolTip="{x:Static loc:Resources.Market_Refresh}" Cursor="Hand">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
            <Button HorizontalAlignment="Right" Margin="0,10,0,0" Style="{StaticResource MaterialDesignFlatButton}" theme:UiMotion.PressScale="True" Foreground="{DynamicResource AccentBrush}" Command="{Binding DownloadAllCommand}" Content="{x:Static loc:Resources.Market_DownloadAll}" Cursor="Hand"/>
        </StackPanel>

        <!--列表-->
        <Border Grid.Row="2" Style="{StaticResource GlassCard}" Padding="6">
            <Grid>
                <ListView x:Name="ItemsList" ItemsSource="{Binding View}" SelectedItem="{Binding SelectedItem}"
                          Background="Transparent" BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Type ListViewItem}}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Margin" Value="2,2"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource GlassSurfaceHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource GlassCardBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Style="{StaticResource MutedText}" TextTrimming="CharacterEllipsis" Text="{Binding Group}"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="8,0" Style="{StaticResource MutedText}" Text="{Binding SizeText}"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="8,0" Foreground="{DynamicResource AccentBrush}" Text="{Binding DownloadMark}"/>
                                <Button Grid.Column="3" Style="{StaticResource MaterialDesignFlatButton}" theme:UiMotion.PressScale="True" VerticalAlignment="Center" Foreground="{DynamicResource AccentBrush}" Content="{x:Static loc:Resources.Market_Download}" Click="Download_Click" Cursor="Hand"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource MutedText}" Text="{x:Static loc:Resources.Market_Empty}"
                           Visibility="{Binding EmptyVisible, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
