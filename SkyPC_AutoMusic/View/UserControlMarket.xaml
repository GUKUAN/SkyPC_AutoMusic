<UserControl x:Class="SkyPC_AutoMusic.View.UserControlMarket"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SkyPC_AutoMusic.View"
             xmlns:loc="clr-namespace:SkyPC_AutoMusic.Properties"
             xmlns:viewmodel="clr-namespace:SkyPC_AutoMusic.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:MarketViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600" d:Background="#FFE5E5E5">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--标题与状态-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Margin="10" Foreground="{DynamicResource PrimaryHueMidBrush}" FontSize="20" Text="{x:Static loc:Resources.Market_Header}"/>
            <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0" Opacity="0.7" TextTrimming="CharacterEllipsis" Text="{Binding StatusText}"/>
        </Grid>

        <!--工具栏-->
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <!--第一行：筛选 + 图标按钮-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!--分类-->
                <ComboBox Grid.Column="0" Width="110" VerticalAlignment="Center" Margin="0,0,6,0" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup, Mode=TwoWay}" materialDesign:HintAssist.Hint="{x:Static loc:Resources.Market_Category}"/>
                <!--子类-->
                <ComboBox Grid.Column="1" Width="110" VerticalAlignment="Center" Margin="0,0,6,0" ItemsSource="{Binding Subs}" SelectedItem="{Binding SelectedSub, Mode=TwoWay}" materialDesign:HintAssist.Hint="{x:Static loc:Resources.Market_SubCategory}"/>
                <!--搜索-->
                <TextBox Grid.Column="2" MinWidth="100" VerticalAlignment="Center" Margin="0,0,6,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="{x:Static loc:Resources.Market_SearchHint}" materialDesign:TextFieldAssist.HasClearButton="True" Style="{StaticResource MaterialDesignFilledTextBox}"/>
                <Button Grid.Column="3" Style="{StaticResource MaterialDesignIconButton}" Background="{DynamicResource MaterialDesignTextFieldBoxBackground}" Margin="4,0" Command="{Binding OpenRepoCommand}" ToolTip="{x:Static loc:Resources.Market_Repo}" Cursor="Hand">
                    <materialDesign:PackIcon Kind="Github"/>
                </Button>
                <Button Grid.Column="4" Style="{StaticResource MaterialDesignIconButton}" Background="{DynamicResource MaterialDesignTextFieldBoxBackground}" Margin="4,0" Command="{Binding RefreshCommand}" ToolTip="{x:Static loc:Resources.Market_Refresh}" Cursor="Hand">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
            <!--第二行：批量-->
            <Button HorizontalAlignment="Right" Margin="0,6,0,0" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding DownloadAllCommand}" Content="{x:Static loc:Resources.Market_DownloadAll}" Cursor="Hand"/>
        </StackPanel>

        <!--列表-->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Opacity="0.95">
            <Grid>
                <ListView x:Name="ItemsList" ItemsSource="{Binding View}" SelectedItem="{Binding SelectedItem}"
                          Background="{DynamicResource MaterialDesignTextFieldBoxBackground}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Type ListViewItem}}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <!--曲名 + 分类-->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis" Text="{Binding Group}"/>
                                </StackPanel>
                                <!--大小-->
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="8,0" Opacity="0.7" Text="{Binding SizeText}"/>
                                <!--已下载-->
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="8,0" Foreground="{DynamicResource PrimaryHueMidBrush}" Text="{Binding DownloadMark}"/>
                                <!--下载-->
                                <Button Grid.Column="3" Style="{StaticResource MaterialDesignFlatButton}" VerticalAlignment="Center" Content="{x:Static loc:Resources.Market_Download}" Click="Download_Click" Cursor="Hand"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" Text="{x:Static loc:Resources.Market_Empty}"
                           Visibility="{Binding EmptyVisible, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
